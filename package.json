{
  "name": "github-cli",
  "version": "0.0.1",
  "private": true,
  "packageManager": "bun",
  "scripts": {
    "prepare": "husky",
    "build": "bun build packages/gh/src/index.ts --target=node --format=cjs --outfile=packages/gh/dist/shim.js",
    "test": "bun test"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.5.0",
    "prettier": "^3.9.6"
  }
}
